{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the image viewer window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
